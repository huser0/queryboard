[package]
name = "oracle-probe"
version = "0.1.0"
edition = "2024"

[dependencies]
asupersync = { version = "=0.3.9", features = ["tls"] }
oracledb = "0.9.1"
tokio = { version = "1.53.1", features = ["rt-multi-thread", "macros", "time"] }
